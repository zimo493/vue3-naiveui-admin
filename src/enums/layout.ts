/**
 * 菜单布局枚举
 */
export const enum LayoutMode {
  /**
   * 左侧菜单布局
   */
  LEFT = "left",
  /**
   * 顶部菜单布局
   */
  TOP = "top",

  /**
   * 混合左侧菜单布局
   */
  MIX_LEFT = "mixLeft",
  /**
   * 右侧菜单布局
   */
  RIGHT = "right",
  /**
   * 混合右侧菜单布局
   */
  MIX_RIGHT = "mixRight",
}
